# 界面设计说明：石墨铭牌

前端的视觉与交互规范。令牌的**唯一权威来源**是
[`frontend/src/styles/tokens.css`](frontend/src/styles/tokens.css)，
本文档说明它为什么长这样，以及使用规则。

## 1. 为什么是"石墨铭牌"

这是一个 Operate 型界面（每天要用的工具），不是展示型页面。它有两个必须成立的性质：

1. **能长时间看表格**。教务人员白天在办公室日光灯下连着看几小时名单与成绩，学生在宿舍晚上查课表。两段使用场景都以亮面为主。
2. **信息必须分层清楚**。一屏里同时有"制度结构"（导航、分区、表头）与"内容"（课程、成绩、条款）。两者如果只靠深浅灰区分，看久了会糊成一片。

所以方案是：**结构面用接近黑的冷石墨，内容面用冷瓷白，明度差拉满**；
铭牌（课程代码、条号、封条）是这套语言里的"标牌"，承担定位作用。

高级感来自价值对比与克制，不是把颜色调得更好看：

- 全站只有一个点缀色，饱和度压在 60% 以下，只出现在状态与主操作上；
- 中性色一律冷调，不掺暖灰——暖奶油底配高饱和点缀色是最常见的机器生成配色；
- 全站直角，无阴影，用 1px 线分层。

## 2. 令牌

### 色彩

| 令牌 | 值 | 用途 |
| --- | --- | --- |
| `--structure` | `#1c2024` | 顶栏、左侧索引列、铭牌底色 |
| `--structure-deep` | `#131619` | 结构面上的下沉区域 |
| `--structure-line` | `#31373c` | 结构面上的分隔线 |
| `--ground` | `#eaecee` | 页面底 |
| `--face` | `#f5f7f8` | 内容面 |
| `--face-raised` | `#fcfdfd` | 内容面上的浮起块 |
| `--ink` | `#15191c` | 正文 |
| `--ink-muted` | `#545e64` | 次要文字 |
| `--muted` | `#98a3a9` | 结构面上的次要文字 |
| `--line` / `--line-strong` | `#d5dadd` / `#abb3b8` | 分隔线 |
| `--accent` | `#96591a` | 点缀色（青铜）：主操作、封条、当前项 |
| `--accent-deep` | `#784612` | 点缀色的按下态 |
| `--ok` / `--warn` / `--bad` | `#2f6b4f` / `#96591a` / `#a03024` | 语义状态 |

对比度已逐项核算（sRGB 相对亮度公式）：

| 组合 | 对比度 |
| --- | --- |
| `--ink` on `--face` | 约 16.5:1 |
| `--ink-muted` on `--face` | 约 6.2:1 |
| `--ink-muted` on `--ground` | 约 5.6:1 |
| `--face` on `--structure` | 约 15.2:1 |
| `--muted` on `--structure` | 约 6.4:1 |
| `--accent` on `--face` | 约 5.2:1 |
| `--face` on `--accent` | 约 5.2:1 |

### 字体

- 正文：PingFang SC / HarmonyOS Sans SC / 思源黑体 / 微软雅黑，回落到 system-ui。
- 等宽只用于**编码、数字与度量**（课程代码、学号、教学周、绩点），不当技术感的装饰。
- 字号比例 1.14–1.2：`12 / 13 / 14 / 16 / 20 / 28`。产品界面不需要夸张对比。

### 间距与形状

- 间距 `4 / 8 / 12 / 16 / 24 / 32 / 48`，阶梯固定，不随手写奇数。
- `--radius: 0`：全站直角。圆角只出现在加载指示的图形上，不作为造型语言。
- 层级只用三个 z 值：`sticky=10`、`overlay=20`、`toast=30`。
- 动效：指数缓出 `cubic-bezier(0.16, 1, 0.3, 1)`，时长 180ms；从已可见的默认态出发，不做"从无到有"的入场动画。

## 3. 组件与交互规则

不用组件库，10 个自建组件覆盖全部界面需求。理由：这套视觉里最关键的"铭牌""封条"不是通用组件的默认造型，套组件库反而要写更多覆盖样式。

| 组件 | 职责 | 规则 |
| --- | --- | --- |
| `AppShell` | 顶栏 + 左侧索引列 + 内容区 | 顶栏三段：我是谁（系统名）、现在在哪（学期/教学周/当前页）、我要做什么（主操作 + 账号 + 退出） |
| `Plate` | 内容分区 | 分区标题 + 说明 + 内容 |
| `DataTable` | 列表 | 表头等宽字符、数字右对齐、行高固定 |
| `StateBlock` | 加载/空/错误三态 | 加载用骨架条（预告内容形状）而不是居中转圈；空态负责教人怎么填满；错误态要说清问题与补救办法 |
| `StateHost` | 三态容器 | 统一 loading/ready/empty/error 四态切换 |
| `StatusPlate` | 状态铭牌 | 状态词用铭牌呈现，不靠颜色单独表意 |
| `TapeMark` | 封条 | 用于"名额已满""与 X 冲突"这类**不允许操作**的标记，替代按钮位 |
| `FieldRow` | 表单行 | 标签 + 提示 + 控件 + 错误 |
| `Btn` | 按钮 | solid（主操作）/ quiet（次操作）；一屏只有一个 solid |
| `ToastHost` | 通知 | 底部居中，成功/失败两态 |

### 交互约定

1. **一屏一个主操作**。位置条右端的主操作由路由决定，其余操作降级为次操作。
2. **不可用的操作给理由**。冲突课程不给"选课"按钮，而是显示"与 X 冲突"封条；满员班显示"名额已满"。
3. **业务规则不在界面里推算**。冲突结论来自服务端预检接口，界面只呈现。
4. **空态说下一步**，不写"暂无数据"这种等于没说的话。
5. **错误态给补救动作**：错误块带"重新加载"。

## 4. 无障碍与一致性检查

- 所有正文组合对比度 ≥ 4.5:1（见上表）。
- 加载态 `aria-busy`，通知 `role="status"`，错误块 `role="alert"`。
- 键盘可达：表单控件与按钮均为原生元素，焦点样式保留。
- 表意不只靠颜色：冲突用封条文字、状态用铭牌文字。

## 5. 改动这份设计的检查清单

改配色或间距之前先过一遍：

1. 新的颜色组合对比度是否 ≥ 4.5:1？
2. 是否新增了第二个点缀色？（全站只允许一个）
3. 是否引入了圆角或阴影？（全站直角、无阴影）
4. 是否把等宽字体用在了非编码/非数字的位置？
5. 是否新增了 z 值层级？（只允许三个）
6. 主操作是否仍然只有一个？
